// eslint-disable-next-line react/prop-types
const Test = ({ name, age }) => {
    return (
      <div>
        <h1>{name}</h1>
        <h2>{age}</h2>
      </div>
    );
  }
  
  export default Test;
