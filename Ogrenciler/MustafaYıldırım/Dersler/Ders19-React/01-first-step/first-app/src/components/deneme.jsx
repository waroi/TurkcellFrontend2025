import React from 'react'

export const Deneme = ({ isim, yas }) => {
    return (
        <>
            <div>{isim}</div>
            <div>{yas}</div>
        </>
    );
};

export default Deneme;

